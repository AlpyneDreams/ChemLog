import { StyleSheet } from 'react-native'

const CommonStyles = StyleSheet.create({
  fab: {
    position: 'absolute',
    margin: 16,
    marginBottom: 24,
    right: 0,
    bottom: 0
  }
})

export default CommonStyles
